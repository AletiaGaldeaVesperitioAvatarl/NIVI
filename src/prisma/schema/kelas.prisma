model Kelas {
  id          Int        @id @default(autoincrement())
  namaKelas   String

  // ✅ SANTRI
  santri      User[]     @relation("SantriKelas")

  // ✅ PENGAJAR
  pengajar    User[]     @relation("PengajarKelas")

  absensi     Absensi[]
  izin        Izin[]
  tugas       Tugas[]
}
