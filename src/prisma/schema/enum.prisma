enum Role {
  admin
  pengajar
  santri
}

enum StatusAbsensi {
  hadir
  izin
  alpha
  sakit
}

enum StatusIzin {
  menunggu
  disetujui
  ditolak
}

enum StatusSubmission {
  submitted
  reviewed
  rejected
}
